// import {writeGameSettings} from '@reducers/game-params/dispatchers';

export const initWriteSettings = (initializedSettings:{}) => {
    // writeGameSettings(initializedSettings);
    console.log(initializedSettings);
};